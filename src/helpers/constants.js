// lazy loading variables
export const size = 5
export const cushion = 1
// increasing cushion would make it more smooth, 
// but for some reason it'd break the initial fetch
// already fetching the past, causing a layout shift and 
// breaking autoscroll